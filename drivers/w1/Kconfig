#
# W1 subsystem configuration
#

menu "1-Wire support"

config W1
	bool "Enable 1-Wire controllers support"
	depends on DM
	help
	  Support for the Dallas 1-Wire bus. This is a low bandwith bus that
	  requires a single line to operate.

if W1

endif

endmenu
